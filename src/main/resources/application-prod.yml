spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/sfg_prod
    username: root
    password: libesc00
    platform: mysql
  jpa:
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: false
